post:
  operationId: buyCellPackage
  tags:
    - Services
  summary: Buy recharge package
  x-verification-status: legacy-doc
  description: Purchase a mobile data or recharge package through Cobru.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/BuyPackageRequest.yaml
  responses:
    '200':
      description: Package purchase result
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    default:
      $ref: ../components/responses/GenericError.yaml
