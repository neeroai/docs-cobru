---
title: API de autenticación
description: Páginas de referencia para la autenticación de Cobru y el refresh del token.
icon: ShieldCheck
---

La API de Cobru usa un modelo por capas:

- `Api-Token`
- `Api-Secret-Key`
- `Authorization: Bearer {access}`

## Endpoint disponible

| Endpoint | Método | Estado |
|----------|--------|--------|
| `/token/refresh/` | `POST` | verificado contra comportamiento real de sandbox |

## Notas para implementadores

- El campo del body es `refresh`, no `refresh_token`.
- Mantén las tres credenciales del lado del servidor.
- Cachea el access token máximo 50 minutos.

Continúa con `Refresh token` para el detalle completo del request y response.
